package com.flatwhite.template.coroutineexample.service

import org.springframework.stereotype.Service

@Service
class OrderService {
    fun getCartItem(cartId: String) {
    }

    fun getUserAddress(userId: String) {
    }

    fun getNearestStock(
        address: String,
        items: String,
    ) {
    }

    fun reserveStock(
        address: Unit,
        stockId: Unit,
    ) {
    }
}
